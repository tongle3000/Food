import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex:1,
        paddingVertical: 30,
        paddingHorizontal: 16,
    },
    title: {
        lineHeight: 30,
        marginBottom:10,
        fontSize: 16,
        alignItems: 'center',
        fontWeight: 'bold'
    },
    
    Item: {
        flexDirection:'row',
        alignItems: 'center'
    },
    subTitle: {
        fontSize: 14,
    },
})
